@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApsisYönetim.Core.Entities.User> SignInManager
@inject UserManager<ApsisYönetim.Core.Entities.User> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    
    <li class="nav-item">
        @{
            var username = "Hello" + User.Identity.Name;
            @Html.ActionLink(username, "Index", "Home",new { @class = "nav-link text-dark" })
        }
    </li>
    <li class="nav-item">
        @Html.ActionLink("Çıkış Yap", "LogOut", "User", new { username = User.Identity.Name }, new { @class = "nav-link text-dark" })
        @*<form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>*@

    </li>
}
else
{
    
    <li class="nav-item">
        @Html.ActionLink("Login","Login","Home",null, new { @class = "nav-link text-dark" })
    </li>
}
</ul>
